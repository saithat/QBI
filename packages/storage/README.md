# HiveBlot storage package

`hiveblot_storage` owns immutable artifact publication, media validation, S3-compatible object
storage, and PostgreSQL persistence mappings. It contains no western blot domain logic and does
not import an application entry point.
