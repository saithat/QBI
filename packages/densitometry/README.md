# HiveBlot densitometry package

`hiveblot_densitometry` owns deterministic pixel measurement, western-blot-specific QC,
source-geometry resolution, and pipeline publication. It never delegates numerical intensity to a
model and does not own HTTP schemas, reviewer mutation, object storage, or generic job execution.
