# Infrastructure

Infrastructure definitions and deployment notes live here. Root `Dockerfile` and
`compose.yaml` remain at their historic paths for compatibility. Kubernetes and Terraform are
explicitly outside PRD-001.
