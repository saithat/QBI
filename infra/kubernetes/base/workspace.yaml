apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hiveblot-job-workspaces
  annotations:
    hiveblot.io/data-class: ephemeral-attempt-scratch
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
