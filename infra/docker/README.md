# Local Docker runtime

The local stack is defined by the root `Dockerfile` and `compose.yaml`. They run PostgreSQL,
vLLM, and the API/viewer. Compose is a development runtime, not a durable production design.
