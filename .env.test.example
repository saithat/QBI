# Explicit isolated test profile. Copy to a temporary env file; do not reuse deployed data.
HIVEBLOT_ENV=test
DATABASE_URL=postgresql://hiveblot_test:hiveblot_test@localhost:5433/hiveblot_test
VLLM_BASE_URL=http://localhost:8001/v1
VLLM_MODEL=test-model
VLLM_API_KEY=test-only
HIVEBLOT_DATA_DIR=/tmp/hiveblot-test-data
S3_ENDPOINT_URL=http://localhost:9002
S3_REGION=us-east-1
S3_BUCKET=hiveblot-test-artifacts
S3_ACCESS_KEY_ID=test-only
S3_SECRET_ACCESS_KEY=test-only-secret
SOURCE_INGEST_ALLOWED_HOSTS=source.test
